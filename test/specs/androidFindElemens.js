describe('Basics of locators in WDIO Appium', ()=> {

    it('Accessibility id',async ()=>{

        const appOption = await $('~App')
        await appOption.click()

        // expect(ac)
    })
})